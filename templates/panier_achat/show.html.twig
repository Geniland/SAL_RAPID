{% extends 'base.html.twig' %}

{% block title %}PanierAchat{% endblock %}

{% block body %}
    <h1>PanierAchat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panier_achat.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ panier_achat.Nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ panier_achat.prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ panier_achat.image }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ panier_achat.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_panier_achat_index') }}">back to list</a>

    <a href="{{ path('app_panier_achat_edit', {'id': panier_achat.id}) }}">edit</a>

    {{ include('panier_achat/_delete_form.html.twig') }}
{% endblock %}
